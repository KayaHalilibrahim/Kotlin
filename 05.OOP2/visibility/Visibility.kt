package com.hkaya.a05oop2

fun main(){

   /*
  - Public = Available from all classes and packages(Class,method,variable).
  - Internal = Only the classes in the module can acces it(method,variable).
  - Private = Can only be accessed from the class in which in which they are defined(method,variable)
  - Protected = Can be aaccessed when they are generated in their current class ot from their current class(method,varable).


    */

}